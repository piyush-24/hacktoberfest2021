#include<bits/stdc++.h>
using namespace std;
 
int main() 
{
	int t;
	cin>>t;
	while(t--)
	{
	    int k;
	    cin>>k;
	    vector<int> v;
	    for(int i=0;i<k;i++)
	    {
	        int x;
	        cin>>x;
	        v.push_back(x);
	    }
	  deque<int> m;
	  m.push_back(v[0]);
	  if(k>1 && m.back()>v[1] )
	        m.push_front(v[1]);
	        else if(k>1)
	        m.push_back(v[1]);
	  for(int i=2;i<k;i++)
	    {
	        if(m.front()<v[i])
	        m.push_back(v[i]);
	        else
	        m.push_front(v[i]);
	    }
	    while(!m.empty())
	    {
	        cout<<m.front()<<" ";
	        m.pop_front();
	    }
	    cout<<endl;
	  
	}
	return 0;
}
// Author : Piyush Jena
